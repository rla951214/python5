from .cat import Cat # . <- "이 폴더 안에 있는" cat.py "라는 파일에서 Cat 이라는 클래스를 가져온다.
from .dog import Dog # . <- "현재 폴더에 있는" dog.py "라는 파일에서 Dog 라는 클래스를 가져온다.
from .horse import Horse # . <- 현재 폴더에 있는" horse.py "라는 파일에서 Horse 라는 클래스를 가져온다.
from .snake import Snake # . <- 현재 폴더에 있는" snake.py"라는 파일에서 Snake 라는 클래스를 가져온다.